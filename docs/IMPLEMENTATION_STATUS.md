# 实施状态

> 更新时间：2026-08-09

## 当前里程碑

公开测试版加固：阶段 1–5 已通过自动化门槛，正在进入阶段 6（验证基础设施）。完整分阶段安排见 [公开测试版执行计划](PUBLIC_BETA_PLAN.md)。

旧文档仍将当前状态描述为 P0，但代码已经包含原生 UI、进程内播放、SMTC、在线元数据和 ListenBrainz 持久队列，因此按代码事实调整里程碑；不重复已经完成的底层验证。

## 已完成

- 初始化 C++20/CMake 工程。
- 添加 Visual Studio 2026 x64 和 Ninja/MSVC 构建预设。
- 添加空的 vcpkg manifest，为后续按项目锁定第三方依赖预留入口。
- 实现 CDDA 常量及 MSF、绝对扇区、LBA、采样帧转换。
- 实现转换范围和乘法溢出检查。
- 实现平台无关 TOC 模型、轨号连续性、轨道位置和 lead-out 验证。
- 从 TOC 自动计算轨道结束 LBA、整盘起始采样帧和轨道采样帧数。
- 实现 Windows 光驱枚举。
- 使用 `IOCTL_CDROM_READ_TOC_EX` 读取原生 TOC 并转换为平台无关模型。
- 实现命令行光驱探针。
- 定义平台无关的 CDDA 扇区源接口。
- 实现带 32 扇区内部缓存的整盘连续 CDDA 流，在减少重叠校验回读的同时保持 Windows 原始 CDDA 接口兼容。
- 连续流支持任意采样帧位置定位，不需要在轨道边界重建数据源。
- 使用合成递增帧序列验证跨轨读取无漏帧、插帧和重复帧。
- 使用 `IOCTL_CDROM_RAW_READ` 和 CDDA 模式实现 Windows 原始音频扇区读取。
- 实现原始 CDDA 读取诊断探针。
- 确认 Windows 原始 CDDA 数据可直接按 PCM16LE 使用，并实现显式格式适配接口。
- 实现事件驱动的 WASAPI 共享模式输出，输入格式固定为音频 CD 原生的
  44.1 kHz、16 位、双声道 PCM。
- 实现播放后台组件：自动选择已就绪的音频 CD，可指定音轨和播放时长。
- 实现预分配的有界 SPSC PCM16 环形缓冲，生产和消费操作均不分配内存、不阻塞。
- 将光盘读取移至独立生产线程，以约 6 秒缓冲和 3 秒首播水位隔离光驱抖动。
- 播放范围可覆盖连续音频轨区间；轨道边界不重建扇区源、连续流或 WASAPI 会话。
- 增加 `--all`、音轨内起始偏移和 `Ctrl+C` 取消；取消可中断 WASAPI 事件等待。
- 实现 Win32、Direct2D 与 DirectWrite 原生播放器界面，接入真实 TOC、曲目选择、播放控制、进度定位和光驱弹出。
- 实现 Windows CD-TEXT 读取、pack 排序与跨 pack 文本拼接，并将专辑、艺术家和曲名接入界面。
- 实现 MusicBrainz TOC 模糊匹配、候选版本时长评分和后台元数据补全。
- MusicBrainz 自动采用要求平均每轨误差不超过 2 秒且任一轨不超过 5 秒；换盘后丢弃上一张盘的迟到结果。
- 实现 GnuDB/CDDB Disc ID 计算、HTTPS `query`/`read` 两阶段查询和协议 6 UTF-8 xmcd 解析；仅采用精确匹配结果。
- 实现 iTunes 专辑及曲目查询，并以专辑/艺术家、音轨数、编号和 TOC 时长差做保守校验。
- 实现 MusicBrainz 与 GnuDB 并行查询、iTunes 条件补全的在线元数据聚合器；界面显示实际命中的来源。
- 接入 Cover Art Archive 1200px 正面封面，并通过 WIC 圆角渲染和本地应用数据缓存复用。
- 原始 CDDA 读取改用 Windows 重叠 I/O，取消时通过 `CancelIoEx` 中断待处理读取。
- WASAPI 输出改为预填后显式启动，正常 EOS 才排空，取消和错误直接停止。
- 消费端检查 WASAPI padding；非 EOS 状态耗尽端点缓冲会记录为欠载失败。
- 实现平台无关的可靠 CDDA 扇区源：单次逻辑读取最多尝试 3 次，连续块额外读取
  1 个重叠扇区并逐字节校验，不一致时重新读取且不向播放端交付可疑数据。
- 将取消状态与普通 I/O 错误分离，避免换盘或停止播放后继续无意义重试。
- 播放后台组件输出逻辑读取、设备读取、重试、重叠校验、不一致、生产端饥饿、
  WASAPI 端点欠载和最低排队帧数等诊断统计。
- 实现平台无关的正式播放状态机，覆盖打开、缓冲、播放、排空、停止、完成、
  取消和失败状态；非法事件不会改变当前状态。
- 将光驱选择、连续流、生产线程、环形缓冲、WASAPI 消费、取消和统计收敛到可复用的
  `CddaPlaybackEngine`；对外提供阻塞式播放、跨线程停止请求和采样帧进度快照。
- 命令行播放后台组件缩减为参数解析、控制台取消和诊断结果展示，不再持有音频管线实现。
- 图形界面改为在工作线程中直接调用进程内播放引擎；播放、停止、定位、切轨、刷新、
  弹出和窗口关闭均走协作取消，不再创建或强制终止播放子进程。
- 播放进度新增实际渲染帧计数：使用已提交帧减去 WASAPI padding，正常排空后精确落到
  目标采样帧；UI 据此更新当前曲目和曲内进度，不再依赖墙上时间推测。
- 接入 Win32 桌面应用 SMTC：同步曲目、艺术家、专辑、播放状态和时间轴，并处理系统
  播放、暂停、停止、上一首、下一首及定位请求。
- 实现同一 WASAPI 客户端内的真实暂停/恢复：暂停仅停止端点时钟且不重置已排队帧，
  恢复后继续原会话；播放中选择、上一首和下一首会自动继续播放目标曲目。
- 在进度条下方显示当前曲内时间和总时长；实现可点击、拖动和滚轮调节的真实音量控件。
- 音量增益在 CDDA 环形缓冲消费端、提交 WASAPI 前执行，单位增益走精确旁路，不改变样本。
- 新增本地用户设置：首次启动音量为 100%，持久化音量、ListenBrainz 上报选项，以及
  按 TOC 键控的当前曲目和精确采样帧位置；播放位置记忆与 Windows 媒体控制固定启用，
  ListenBrainz Token 仍仅进入 Windows 凭据管理器。
- 实现平台无关 ListenBrainz 播放会话追踪，按实际渲染采样帧累计播放量；支持
  `playing_now` 以及达到半曲或 4 分钟（取较短者）后的 `single`。
- 实现 ListenBrainz 后台 HTTPS 上报；Token 优先读取 Windows 凭据管理器，环境变量仅作
  临时回退，Token 不进入仓库文件和日志。
- 接入 `/1/validate-token`、`playing_now`、`single` 与按客户端清理正在播放状态；解析
  认证、HTTP 和速率限制结果，并向 UI 暴露连接、同步、重试和失败状态。
- 正式播放记录先写入 WinSQLite 持久队列；网络错误和 5xx 使用带轻量抖动的指数退避，
  429 遵循 `X-RateLimit-Reset-In`，401 停止发送直至凭据更新。队列按不可逆 Token
  指纹隔离账户且不保存 Token，应用重启后继续补报。
- MusicBrainz 匹配结果现在保留 release、release group、track、recording 和 artist
  MBID，ListenBrainz 载荷在可用时连同轨号、曲长和客户端标识一并提交。
- 播放器主界面不显示 ListenBrainz 上报进度；设置页显示已验证账户、
  待同步数量、失败数量和下一次重试倒计时。
- 右上角新增独立 ListenBrainz 设置页；使用密码输入框新增、替换或移除 Token，保存后
  热重载上报器，无需重启应用。无需配置的播放与系统能力不在设置页展示。
- 添加基础单元测试并接入 CTest。
- 增加可注入的设备生命周期消息分类和平台无关播放恢复协调器；光盘刷新期间的新通知会合并，旧快照不会覆盖最后一次介质状态。
- 休眠前协作停止并保存精确位置；唤醒后重新验证 TOC，仅当 Disc 设置键仍一致时恢复先前正在播放的会话，暂停态或换盘不会自动播放。
- 默认 WASAPI 端点返回设备/资源失效或音频服务未运行时，从实际渲染位置自动重开一次；立即再次失败会停止重试并显示明确错误，稳定播放一秒后才重新开放恢复机会。
- 将介质移除类 Win32 错误与 CRC 等严格读取失败分开；前者刷新失效 UI，后者继续作为读取错误暴露，不生成替代样本。
- 播放退出顺序现在先停止 WASAPI、结束跨线程取消观察，再显式关闭 COM 音频资源。
- 播放引擎现在从所选曲目所在的完整连续音频区间打开 CDDA 源；同一区间内的前后切轨和随机定位通过线程安全命令进入原播放线程，不重新创建 `WasapiOutput` 或 `IAudioClient`。
- 会话内定位先停止生产者并重置端点缓冲，再清空环形缓冲、精确定位连续 CDDA 流和重新预缓冲；旧时间线样本不能越过定位屏障。暂停中定位保留暂停状态。
- 进度快照增加流代次、基准曲目和基准曲内帧；UI、SMTC 和 ListenBrainz 使用新基准等待定位生效，不把旧进度套到新曲目。
- 混合模式跨数据轨不复用 CDDA 读取范围，返回 `outside_session` 并由 UI 走原有安全重建路径；数据扇区不会进入音频管线。
- 枚举活动 WASAPI 渲染端点的稳定 ID、友好名称和默认标记；设置页允许选择系统默认或具体设备并持久化选择，设备消失时保留选择并显示明确错误。
- 共享模式继续作为默认输出，固定输入为 44.1 kHz/16 位/双声道 PCM并允许 Windows 转换；独占模式仅在用户显式启用且设备精确支持该格式时初始化。
- 独占格式不支持、设备占用和初始化失败均保留原始 HRESULT 与中文解释；只有显式启用回退时才重新激活共享客户端，结果同时记录请求模式、实际模式、回退原因和是否尝试回退。
- WASAPI 会话初始化失败和回退会重建音频客户端；停止顺序为停止端点、释放渲染客户端/音频客户端/设备、关闭事件句柄，最后释放枚举器和 COM 所有权。
- 实现标准 MusicBrainz Disc ID：按首末轨号、lead-out 和 99 个轨偏移生成大写十六进制 SHA-1 输入，并使用 MusicBrainz 的 `.`/`_`/`-` Base64 变体；在线查询先访问精确 Disc ID，未关联时才回退现有 TOC 模糊查询。
- 当前 TOC 模型缺少多会话音频 lead-out，遇到数据轨时拒绝伪造精确 Disc ID并保留模糊后备；纯音频盘按标准路径查询。
- MusicBrainz XML 解析保留全部通过音轨数与时长校验的发行版，精确查询候选优先于模糊后备；按光盘键记忆所选 release MBID，播放器按 `M` 可循环重新选择。
- 建立字段级来源模型和确定性优先级：用户修订、CD-TEXT、MusicBrainz、GnuDB、iTunes、未知依次降级；低优先级刷新不能覆盖 CD-TEXT，任何远端刷新都不能覆盖用户值。
- 增加版本化元数据缓存，按不含路径/标题的 TOC 键存放于当前用户应用数据目录；支持离线加载、原子写入、大小/键校验、损坏回退、过期判断和旧无版本格式迁移。
- 主界面来源摘要显示专辑名、专辑艺术家、当前曲名和表演者的逐字段来源；`F2` 组合键提供专辑/曲目字段手动修订并持久化，屏幕输入框支持 Enter 保存和 Esc 取消。
- ListenBrainz 播放会话在首个 `playing_now` 之后遇到迟到 MusicBrainz 身份时只补发一次修正；后续 MBID 继续补全不会重复 `playing_now`，正式 `single` 仍只持久入队一次。
- 将 WinHTTP 封装为可替换 `HttpClient`，ListenBrainz 上报器可注入假传输、临时队列路径和内存 Token；正式路径继续使用 WinHTTP、Windows 凭据管理器与当前用户 SQLite。
- 将持久队列提取为独立接口，模式升级到 v3 并增加账户/待重试索引；支持 v1/v2 原地迁移、重启恢复、账户隔离、重试和只清理当前账户。
- 设置页增加“清理当前账户待同步队列”，不删除其他账户数据；播放主界面仍不显示 ListenBrainz 进度。
- 发布工程重构第一批把光盘快照/缓存合并移入 `disc_snapshot.cpp`，把响应式页面几何移入 `ui_layout.cpp`；`main_window.cpp` 保留窗口消息、播放协调和绘制调度，不再直接实现元数据加载或页面布局计算。
- ListenBrainz 凭据格式、凭据管理器/环境加载和保存移入 `listenbrainz_credentials.cpp`；SQLite v3 队列、WinHTTP 客户端此前已分别拆分，旧的内嵌 v2 队列死代码现已移除。上报器只协调提交、重试状态与这些可替换边界。
- 新增容量有界的诊断事件模型和设置页导出入口，只记录状态码、模式、计数及生命周期事件，不采集媒体元数据或账户名。Token/Authorization、进程环境 Token、本机绝对路径和稳定 WASAPI 端点 ID 在写入与导出时双重脱敏；文件路径由原生保存对话框明确选择。
- UI 调色板提取为可测试主题模块，启动和 `WM_THEMECHANGED`/`WM_SETTINGCHANGE` 时跟随 Windows 应用浅色/深色与高对比度系统颜色；画刷、编辑框和 DWM 标题栏/背景材料同步重建，高对比度下禁用背景材料。
- 自绘主窗口通过标准 MSAA 客户端对象和动态窗口名称提供屏幕阅读器基础可达性；焦点变化发送辅助技术事件，主要操作已有键盘路径，`F1` 原生帮助列出播放、切轨、修订、设备、独占/回退和诊断快捷键。公开测试仍需用 Narrator/NVDA 做人工朗读顺序检查。

## 本次验证结果

- CMake 使用 Ninja/MSVC 成功配置。
- MSVC Debug 构建成功。
- `make release` 可完成 Release 配置、全量/增量构建和测试；已验证中文 MSVC
  环境下修改公共头文件会正确触发所有依赖目标重编译。
- CTest：2/2 测试程序通过。平台无关测试覆盖时间转换、边界、溢出、TOC、GnuDB、
  xmcd、CDDA 重试/短读/取消/重叠策略、播放状态机、PCM16 音量和 ListenBrainz 阈值；
  Windows 测试覆盖播放结果语义、无设备访问的请求边界、音量边界、用户设置往返与
  损坏文件回退、SMTC 安全降级、ListenBrainz 两类 JSON 载荷契约及全部诊断枚举名称。
- 2026-08-09 阶段 1 回归：先加入失败测试，确认恢复协调器和错误分类接口缺失；实现后 `make release` 构建成功，CTest 2/2 通过。新增测试覆盖刷新事件合并、同盘唤醒续播、换盘/暂停态禁止自动续播、默认端点单次重试与防循环、WASAPI/介质错误分类、光学卷与电源消息分类。
- 2026-08-09 阶段 2 回归：先加入失败测试，确认会话定位 API 和连续区间规划器缺失；实现后相关测试与 `make release` 通过。新增测试覆盖向前/向后切轨规划、数据轨拒绝、跨数据轨显式回退、曲尾越界，以及 128 次跨边界抖动定位；每次均验证旧环形缓冲归零和目标起连续三帧无丢失、重复或插入。
- 2026-08-09 阶段 3 回归：先加入失败测试，确认输出策略、格式协商和持久设置接口缺失；实现后 `make release` 构建成功，CTest 2/2 通过。假后端覆盖共享默认、精确独占、格式不支持、独占占用、显式共享回退和所选端点失效；设置测试覆盖端点 ID、独占开关、回退开关和安全默认值。
- 2026-08-09 阶段 4a 回归：先加入失败测试，确认标准 Disc ID 和精确查询路径缺失；实现后 `make release` 通过。使用 MusicBrainz 官方 6 轨 TOC 向量验证 Disc ID 为 `49HHV7Eb8UKF3aQiNmu1GR8vKTY-`，并验证精确路径不携带 `toc`、模糊路径只作为后备。
- 2026-08-09 阶段 4b 回归：先加入失败测试，确认字段来源、修订保护、候选记忆和缓存模式缺失；实现后 `make release` 通过。新增测试覆盖来源优先级、用户值刷新保护、候选选择/失效回退、字段来源往返、缓存新鲜度、损坏回退、旧格式迁移，以及双发行版 MusicBrainz XML 全量保留。
- 2026-08-09 阶段 5 回归：先加入失败测试确认 `playing_now` 不会在身份迟到时修正；实现后验证只修正一次且 `single` 不重复。临时 SQLite 测试覆盖 v1→v3 迁移、重启恢复、账户切换隔离和当前账户清理；脚本化假 HTTP 覆盖 401 保留队列、429 遵循 17 秒服务端窗口、503 与网络错误退避。完整 `make release` 通过，测试无公网依赖。
- 2026-08-09 阶段 6 回归：平台无关连续流以种子 `0xCD4045EED` 执行 4,096 次任意定位，Windows 连续会话规划以种子 `0xCD4047A11` 执行 4,096 次任意切轨；逐帧/逐偏移验证旧数据清除、连续帧、会话剩余长度和最新命令顺序。失败时输出种子、迭代和目标参数。
- 同日重新枚举到 3 个活动音频端点。新增探针在默认 Minifuse 端点完成真实共享模式回环：采集 57,330 帧（44.1 kHz 混音格式）、335,290 个非零字节且没有数据不连续标志；真实独占模式提交并排空 44,100 个 44.1 kHz/16 位/双声道精确 PCM 帧。首次独占验证暴露事件驱动独占要求整缓冲包的问题，现改为可取消的轮询独占写入，避免为凑整包伪造/填充样本，并由策略测试锁定共享事件驱动、独占轮询语义。
- 当前仍未枚举到可用光驱/介质，且 CIM 硬件查询此前被拒绝；因此本批未进行真实拔盘、换盘和休眠回归。默认端点切换、独占外部采集连续性和 Windows 10/11 多设备矩阵仍按 `HARDWARE_VALIDATION.md` 补测，不以当前单端点观察替代。
- 2026-08-09 阶段 7a 回归：元数据快照、页面布局和 ListenBrainz 凭据/队列/传输职责拆分后，完整 `make release` 构建成功且 CTest 2/2 通过；既有缓存、队列迁移、假 HTTP、设置和播放行为测试保持全绿。
- 2026-08-09 阶段 7b 回归：诊断测试注入两个 Token、本机用户路径和完整端点 ID，验证内存快照及实际临时导出文件均不含原值，并验证有界环只保留最新事件。完整 `make release` 成功，CTest 2/2 通过。
- 2026-08-09 阶段 7c 回归：确定性调色板测试覆盖浅色、深色和高对比度前景/背景分离；完整 `make release` 成功，CTest 2/2 通过。MSAA、系统主题消息、Narrator/NVDA 朗读和键盘焦点的真实桌面观察列入发布检查表，不伪称由单元测试覆盖。
- 已验证系统光驱枚举能力。
- 无介质时 TOC 读取返回“设备未就绪”；该错误路径工作正常。
- 使用真实音频 CD 成功读取 TOC 和原始 CDDA 数据。
- 使用可靠读取链路完成 5 秒实体光盘播放：12 次逻辑/设备读取交付 384 个扇区，
  11 次连续块重叠校验全部一致，生产端饥饿和 WASAPI 端点欠载均为 0。
- 播放逻辑提取为 `CddaPlaybackEngine` 后再次完成 5 秒实体光盘回归：提交 220,500 帧，
  12 次设备读取、11 次重叠校验均成功，生产端饥饿和端点欠载仍为 0。
- 实体盘协作取消测试通过：播放进入活动状态后由另一线程请求停止，最终状态为
  `cancelled`，没有强制终止进程。
- 实体盘同会话暂停/恢复测试通过：暂停后实际渲染帧在观测窗口内保持不变，恢复后继续
  增长，最终可通过协作取消正常结束。
- 图形界面进程内播放集成回归通过：播放时新增 3 个引擎/生产/取消线程，停止后线程数
  恢复；全程窗口响应正常、旧版 `CD.404.Playback` 子进程数为 0，最终以退出码 0 关闭。
- 原始读取一次成功取得 16 个扇区，共 37,632 字节。
- 连续流在真实光盘上成功跨越相邻音轨读取三个采样帧，期间未重建扇区源。
- 合成跨轨测试使用不规则读取块通过轨道边界，并逐帧验证完整序列。
- PCM 格式测试覆盖有符号边界样本、帧对齐、目标缓冲区容量和空输入。
- 使用真实音频 CD 完成从原始扇区读取、PCM 适配到默认 WASAPI 端点的 5 秒试播，
  所有请求帧均已写入并正常排空。
- 环形缓冲测试覆盖环绕、部分传输、满/空/EOS/reset，并发传输 50,000 帧时逐帧
  验证无丢失、重复和重排。
- 使用 6 秒有界缓冲完成 8 秒实体光盘流式播放，共提交 352,800 帧，验证不依赖
  整段预读。
- 从实体光盘某音轨边界前开始播放并越过边界，共连续提交 264,600 帧；期间保持
  同一个 CDDA 连续流和 WASAPI 会话。
- 使用实体 9 音轨 CD 验证在线链路：MusicBrainz 与 iTunes 均成功匹配同一专辑；GnuDB 服务请求成功，但该光盘没有条目，未产生错误补全。
- 使用无 CD-TEXT 的 9 音轨 Märchen EP 刻录盘复测误匹配：MusicBrainz 返回候选平均每轨误差 22.2 秒、最大 47.1 秒，现已按阈值拒绝，不再显示错误专辑。

受限自动化环境可能阻止 MSBuild FileTracker 访问部分系统资源；遇到这种情况可使用 Ninja/MSVC 预设。该限制不影响普通开发终端使用 Visual Studio 生成器预设。

## 尚未完成

- 实体光盘跨轨连续读取的重复性和抖动硬件复测（合成压力门槛已通过）。
- 使用外部采集验证独占模式的端到端输出连续性（真实独占提交/排空已通过）。
- 在真实光驱和多端点硬件上重复验证拔盘、换盘、默认端点切换和休眠/唤醒路径。

## 下一实施批次

1. 在行为测试保护下拆分主窗口和 ListenBrainz 上报器的职责边界。
2. 增加隐私脱敏诊断导出、主题/高对比度/键盘可达性与屏幕阅读器基础支持。
3. 补齐每用户安装、版本资源、隐私/许可/发布检查表并执行最终发布门禁。
